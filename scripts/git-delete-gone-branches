#!/usr/bin/env bash
set -euo pipefail

# Delete local branches whose upstream tracking branch no longer exists
git list-gone-branches | while read -r branch; do
    echo "Deleting branch: $branch"
    git branch -D "$branch"
done
